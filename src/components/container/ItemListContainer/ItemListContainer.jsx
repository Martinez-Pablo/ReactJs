

export const ItemListContainer = ( {saludo} ) => {
    return (
    // podemos crear un fragment o un div, la diferencia es que fragment no crea un nodo 
        <> 
            ItemListContainer {saludo} 
        </>
    )
}

